export interface LibHelper {
  doSomething(): void;
}

export interface LibConfig {
  enabled: boolean;
  timeout: number;
}

export interface InlinedType {
  value: string;
}

// Internal type that should NOT appear in output
export interface InternalLibType {
  secret: string;
}

// Component declaration that should NOT appear in output
export declare const LibComponent: React.FC<{ config: LibConfig }>;

// Function declaration that should NOT appear in output
export declare function internalHelper(): InternalLibType;

